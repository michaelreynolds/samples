<form class="form-horizontal">
  <div class="form-group">
    <label class="col-sm-1 control-label">Email</label>
    <div class="col-sm-3">
      {{input type="text" value=username placeholder="Username" class="form-control" required="required"}}
    </div>
  </div>
  <div class="form-group">
    <label class="col-sm-1 control-label">Password</label>
    <div class="col-sm-3">
      {{input type="password" value=password autocomplete="off" placeholder="Password" class="form-control" required="required"}}
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-1 col-sm-3">
      <button {{action "loginUser" users}} class="btn btn-default">Sign in</button>
    </div>
  </div>
</form>

{{#if session.isAuthenticated}}
   <a {{action 'logout'}}>Logout</a>
 {{else}}
   {{#link-to 'login'}}Login{{/link-to}}
 {{/if}}

{{yield}}






<!-- <ul>
  <li id="login">
    <a id="login-trigger" {{action "toggleLogin"}}>Log in <span>&#x25BC;</span></a>
    <div id="login-content">
      <form {{action "loginUser" on="submit"}}>
        {{input type="text" value=username placeholder="Username"}}
        {{input type="password" autocomplete="off" value=password placeholder="Password"}}
        {{input type="submit" value=login class="button"}}
      </form>
        <fieldset id="inputs">
          <input id="username" type="text" class="form-control" name="Username" placeholder="Username" autofocus="autofocus" required>
          {{input type="text" value=username placeholder="Username" class="form-control" required="required"}}
          <input id="password" type="password" class="form-control" name="Password" placeholder="Password" required>
          {{input type="password" value=password autocomplete="off" placeholder="Password" class="form-control" required="required"}}
        </fieldset>
        <fieldset id="actions">
          {{input type="submit" value="Log In" class="btn btn-success" id="login-btn"}}
          <button {{action "loginUser" users}} class="btn btn-default" id="login-btn">Log In</button>
          <input type="button" class="btn btn-success" id="login-btn" value="Login" required>
          <input type="button" class="btn btn-danger" id="logout-btn" value="Logout" required>
          <label><input type="checkbox" checked="checked"> Keep me signed in</label>
        </fieldset>
      </form>
    </div>
  </li>
</ul> -->
